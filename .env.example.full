# STORI TRANSACTION PROCESSOR - FULL CONFIGURATION
# Complete environment variables for advanced users and customization

# ═══════════════════════════════════════════════════════════════
# 🔧 MUST CHANGE - Customize these values for your setup
# ═══════════════════════════════════════════════════════════════

# Your email address to receive transaction summaries
TO_ADDRESS=your-email@example.com
TO_NAME=Your Name

# Email delivery method: development (MailHog), gmail (Gmail SMTP)
EMAIL_MODE=development

# Gmail credentials (only needed if EMAIL_MODE=gmail), you use this for becoming able to send real email using your gmail account
GMAIL_USERNAME=your-email@gmail.com  
GMAIL_PASSWORD=your-app-password

# ═══════════════════════════════════════════════════════════════
# 📋 MAINTAIN EXACTLY - Copy these values as-is to your .env file
# ═══════════════════════════════════════════════════════════════

# Application Configuration
ENVIRONMENT=local
DATA_PATH=./testdata

# Database Configuration (LOCAL DEVELOPMENT ONLY)
DB_DRIVER=sqlite

# Email Branding (appears as sender)
FROM_ADDRESS=noreply@stori.com
FROM_NAME=Stori Account Summary

# MailHog Configuration (development mode)
MAILHOG_HOST=mailhog
MAILHOG_PORT=1025

# Gmail Configuration (gmail mode)
GMAIL_HOST=smtp.gmail.com
GMAIL_PORT=587

# AWS SES Configuration (production mode)
SES_FROM_EMAIL=noreply@stori.com
SES_REGION=us-west-2

# Security Configuration
MAX_FILE_SIZE=10485760
ALLOWED_EXTENSIONS=csv
MAX_TRANSACTIONS=100000
MAX_AMOUNT_LIMIT=1000000.00
PROCESS_TIMEOUT=30s